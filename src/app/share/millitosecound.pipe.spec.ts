import { MillitosecoundPipe } from './millitosecound.pipe';

describe('MillitosecoundPipe', () => {
  it('create an instance', () => {
    const pipe = new MillitosecoundPipe();
    expect(pipe).toBeTruthy();
  });
});
